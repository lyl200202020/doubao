# 客户运营管理后台系统 - 第二阶段前端详细设计

## 文档信息

| 项目 | 内容 |
|------|------|
| 版本 | v1.0.0 |
| 阶段 | 第二阶段（v0.0.2-v0.0.4） |
| 创建日期 | 2026-03-07 |
| 编制人 | 前端开发 Agent |
| 文档目的 | 指导第二阶段前端开发实施 |

---

## 一、技术栈

- **框架**: Vue 3.4 + TypeScript
- **构建工具**: Vite 5
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **UI 组件库**: Element Plus
- **HTTP 客户端**: Axios
- **代码规范**: ESLint + Prettier

---

## 二、目录结构

```
src/
├── api/                      # API 接口
│   ├── system/              # 系统管理模块
│   │   ├── org.ts          # 机构管理接口
│   │   ├── user.ts         # 用户管理接口
│   │   ├── menu.ts         # 菜单管理接口
│   │   └── role.ts         # 角色管理接口
│   └── index.ts            # API 统一导出
├── assets/                   # 静态资源
├── components/               # 公共组件
│   ├── common/              # 通用组件
│   │   ├── Pagination.vue   # 分页组件
│   │   ├── SearchForm.vue   # 搜索表单
│   │   └── TableOperator.vue # 表格操作栏
│   └── upload/              # 上传组件
├── config/                   # 配置文件
│   ├── index.ts            # 全局配置
│   └── theme.ts            # 主题配置
├── directives/               # 自定义指令
│   ├── permission.ts       # 权限指令 v-permission
│   └── index.ts            # 指令统一注册
├── hooks/                    # 组合式函数
│   ├── useTable.ts         # 表格逻辑
│   ├── useForm.ts          # 表单逻辑
│   └── usePermission.ts    # 权限逻辑
├── layout/                   # 布局组件
│   ├── index.vue           # 主布局
│   ├── components/          # 布局子组件
│   │   ├── Sidebar.vue     # 侧边栏
│   │   ├── Navbar.vue      # 顶部导航
│   │   └── TagsView.vue    # 标签页
│   └── composables/         # 布局组合式函数
├── router/                   # 路由配置
│   ├── index.ts            # 路由入口
│   ├── routes.ts           # 静态路由
│   └── guard.ts            # 路由守卫
├── stores/                   # Pinia 状态管理
│   ├── index.ts            # Store 入口
│   ├── user.ts             # 用户状态
│   ├── menu.ts             # 菜单状态
│   └── permission.ts       # 权限状态
├── styles/                   # 全局样式
│   ├── index.scss          # 样式入口
│   ├── variables.scss      # 变量定义
│   ├── mixins.scss         # 混入
│   └── element.scss        # Element Plus 覆盖
├── types/                    # TypeScript 类型定义
│   ├── api.d.ts            # API 响应类型
│   ├── menu.d.ts           # 菜单类型
│   ├── user.d.ts           # 用户类型
│   └── org.d.ts            # 机构类型
├── utils/                    # 工具函数
│   ├── request.ts          # Axios 封装
│   ├── auth.ts             # 认证工具
│   ├── validate.ts         # 验证工具
│   └── index.ts            # 工具统一导出
├── views/                    # 页面组件
│   ├── login/              # 登录页
│   │   └── index.vue
│   ├── dashboard/          # 首页
│   │   └── index.vue
│   └── system/             # 系统管理
│       ├── org/            # 机构管理
│       │   ├── index.vue   # 列表页
│       │   └── components/ # 子组件
│       │       └── OrgForm.vue # 表单弹窗
│       ├── user/           # 用户管理
│       │   ├── index.vue
│       │   └── components/
│       │       └── UserForm.vue
│       ├── menu/           # 菜单管理
│       │   ├── index.vue
│       │   └── components/
│       │       └── MenuForm.vue
│       └── role/           # 角色管理
│           ├── index.vue
│           └── components/
│               └── RoleForm.vue
├── App.vue                 # 根组件
└── main.ts                 # 入口文件
```

---

## 三、核心模块设计

### 3.1 权限控制模块（v0.0.2）

#### 3.1.1 类型定义

```typescript
// types/menu.d.ts
export interface Menu {
  menuId: string
  menuName: string
  menuType: 1 | 2 | 3  // 1-目录，2-菜单，3-按钮
  parentMenuId?: string
  menuUrl?: string
  menuIcon?: string
  perms?: string
  sortOrder: number
  visible: 0 | 1
  children?: Menu[]
}

// types/user.d.ts
export interface UserInfo {
  userId: string
  userCode: string
  userName: string
  orgId: string
  orgName: string
  roleId: string
  roleName: string
  authLevel: number
  menus: Menu[]
  perms: string[]
}
```

#### 3.1.2 Pinia Store

```typescript
// stores/user.ts
import { defineStore } from 'pinia'
import { login, logout, getUserInfo } from '@/api/system/user'
import { resetToken, getToken, setToken, removeToken } from '@/utils/auth'
import type { UserInfo } from '@/types/user'

interface UserState {
  token: string
  userInfo: UserInfo | null
  roles: string[]
  permissions: string[]
}

export const useUserStore = defineStore('user', {
  state: (): UserState => ({
    token: getToken() || '',
    userInfo: null,
    roles: [],
    permissions: []
  }),

  getters: {
    userId: (state) => state.userInfo?.userId || '',
    userName: (state) => state.userInfo?.userName || '',
    orgId: (state) => state.userInfo?.orgId || '',
    hasLogin: (state) => !!state.token
  },

  actions: {
    // 登录
    async login(loginForm: { userCode: string; password: string }) {
      try {
        const res = await login(loginForm)
        const { token } = res.data
        this.token = token
        setToken(token)
        return Promise.resolve(res)
      } catch (error) {
        return Promise.reject(error)
      }
    },

    // 获取用户信息和菜单
    async getInfo() {
      try {
        const res = await getUserInfo()
        const { userInfo } = res.data
        
        this.userInfo = userInfo
        this.roles = [userInfo.roleName]
        this.permissions = userInfo.perms || []
        
        return res
      } catch (error) {
        return Promise.reject(error)
      }
    },

    // 登出
    async logout() {
      try {
        await logout()
        this.resetToken()
        return Promise.resolve()
      } catch (error) {
        return Promise.reject(error)
      }
    },

    // 重置 Token
    resetToken() {
      this.token = ''
      this.userInfo = null
      this.roles = []
      this.permissions = []
      removeToken()
    }
  }
})
```

#### 3.1.3 路由守卫

```typescript
// router/guard.ts
import type { Router, RouteRecordRaw } from 'vue-router'
import { useUserStore } from '@/stores/user'
import { useMenuStore } from '@/stores/menu'
import { generateRoutes } from '@/utils/router'
import NProgress from 'nprogress'
import 'nprogress/nprogress.css'

const whiteList = ['/login']

export function setupRouterGuard(router: Router) {
  router.beforeEach(async (to, from, next) => {
    NProgress.start()
    
    const userStore = useUserStore()
    const menuStore = useMenuStore()
    const hasToken = userStore.token
    
    if (hasToken) {
      if (to.path === '/login') {
        next({ path: '/' })
        NProgress.done()
      } else {
        const hasRoles = userStore.roles && userStore.roles.length > 0
        
        if (hasRoles) {
          next()
        } else {
          try {
            // 获取用户信息和菜单
            const { menus, perms } = await userStore.getInfo()
            
            // 生成动态路由
            const accessRoutes = generateRoutes(menus)
            
            // 添加路由
            accessRoutes.forEach(route => {
              router.addRoute(route)
            })
            
            // 保存菜单到 Store
            menuStore.setMenus(menus)
            
            next({ ...to, replace: true })
          } catch (error) {
            // 获取失败，清空 token 并跳转登录页
            await userStore.resetToken()
            ElMessage.error('获取用户信息失败，请重新登录')
            next(`/login?redirect=${to.path}`)
            NProgress.done()
          }
        }
      }
    } else {
      // 没有 token
      if (whiteList.indexOf(to.path) !== -1) {
        next()
      } else {
        next(`/login?redirect=${to.path}`)
        NProgress.done()
      }
    }
  })

  router.afterEach(() => {
    NProgress.done()
  })
}
```

#### 3.1.4 动态路由生成

```typescript
// utils/router.ts
import type { RouteRecordRaw } from 'vue-router'
import type { Menu } from '@/types/menu'

// 布局组件
const Layout = () => import('@/layout/index.vue')

/**
 * 将菜单转换为路由
 */
export function generateRoutes(menus: Menu[]): RouteRecordRaw[] {
  const routes: RouteRecordRaw[] = []
  
  menus.forEach(menu => {
    // 只处理目录和菜单类型
    if (menu.menuType === 1 || menu.menuType === 2) {
      const route: RouteRecordRaw = {
        path: menu.menuUrl || '/',
        name: menu.menuId,
        component: menu.menuType === 1 ? Layout : undefined,
        meta: {
          title: menu.menuName,
          icon: menu.menuIcon,
          perms: menu.perms
        },
        children: []
      }
      
      // 递归处理子菜单
      if (menu.children && menu.children.length > 0) {
        route.children = generateRoutes(menu.children)
      }
      
      routes.push(route)
    }
  })
  
  return routes
}

/**
 * 获取侧边栏菜单（过滤按钮）
 */
export function getSidebarMenus(menus: Menu[]): Menu[] {
  return menus.filter(menu => menu.menuType === 1 || menu.menuType === 2)
}

/**
 * 获取按钮权限列表
 */
export function getButtonPerms(menus: Menu[]): string[] {
  const perms: string[] = []
  
  menus.forEach(menu => {
    if (menu.menuType === 3 && menu.perms) {
      perms.push(menu.perms)
    }
    if (menu.children && menu.children.length > 0) {
      perms.push(...getButtonPerms(menu.children))
    }
  })
  
  return perms
}
```

#### 3.1.5 权限指令

```typescript
// directives/permission.ts
import type { Directive, DirectiveBinding } from 'vue'
import { useUserStore } from '@/stores/user'

/**
 * v-permission 指令
 * 用法：v-permission="['sys:org:add']"
 */
export const permission: Directive = {
  mounted(el: HTMLElement, binding: DirectiveBinding) {
    const { value } = binding
    const userStore = useUserStore()
    const permissions = userStore.permissions
    
    if (value && value instanceof Array && value.length > 0) {
      const hasPermission = permissions.some(perm => value.includes(perm))
      
      if (!hasPermission) {
        // 移除元素
        el.parentNode && el.parentNode.removeChild(el)
      }
    } else {
      throw new Error('请设置权限标签值')
    }
  }
}
```

```typescript
// directives/index.ts
import type { App } from 'vue'
import { permission } from './permission'

/**
 * 注册全局指令
 */
export function setupDirectives(app: App) {
  // 权限指令
  app.directive('permission', permission)
}
```

#### 3.1.6 权限判断 Hook

```typescript
// hooks/usePermission.ts
import { useUserStore } from '@/stores/user'

export function usePermission() {
  const userStore = useUserStore()
  
  /**
   * 检查是否有权限
   */
  const hasPermission = (permission: string): boolean => {
    const permissions = userStore.permissions
    return permissions.includes(permission)
  }
  
  /**
   * 检查是否有任一权限
   */
  const hasAnyPermission = (permissions: string[]): boolean => {
    return permissions.some(perm => hasPermission(perm))
  }
  
  /**
   * 检查是否有所有权限
   */
  const hasAllPermissions = (permissions: string[]): boolean => {
    return permissions.every(perm => hasPermission(perm))
  }
  
  return {
    hasPermission,
    hasAnyPermission,
    hasAllPermissions
  }
}
```

#### 3.1.7 Axios 拦截器

```typescript
// utils/request.ts
import axios, { type AxiosInstance, type AxiosRequestConfig, type AxiosResponse } from 'axios'
import { useUserStore } from '@/stores/user'
import { ElMessage, ElMessageBox } from 'element-plus'
import { getToken } from '@/utils/auth'

// 响应码
const SUCCESS_CODE = 200
const UNAUTHORIZED_CODE = 401
const FORBIDDEN_CODE = 403

// 创建 axios 实例
const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 15000
})

// 请求拦截器
service.interceptors.request.use(
  (config) => {
    // 添加 token
    const token = getToken()
    if (token) {
      config.headers['Authorization'] = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    console.error('Request error:', error)
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, msg, data } = response.data
    
    // 业务错误
    if (code !== SUCCESS_CODE) {
      ElMessage.error(msg || '系统错误')
      
      // 401: 未授权
      if (code === UNAUTHORIZED_CODE) {
        ElMessageBox.confirm('登录状态已过期，请重新登录', '提示', {
          confirmButtonText: '重新登录',
          type: 'warning'
        }).then(() => {
          const userStore = useUserStore()
          userStore.resetToken()
          location.reload()
        })
      }
      
      // 403: 无权限
      if (code === FORBIDDEN_CODE) {
        ElMessage.error('无权限访问')
      }
      
      return Promise.reject(new Error(msg || '系统错误'))
    }
    
    return response
  },
  (error) => {
    console.error('Response error:', error)
    
    // HTTP 错误
    if (error.response) {
      const { status } = error.response
      
      switch (status) {
        case 401:
          ElMessage.error('未授权，请重新登录')
          break
        case 403:
          ElMessage.error('无权限访问')
          break
        case 404:
          ElMessage.error('请求地址不存在')
          break
        case 500:
          ElMessage.error('服务器内部错误')
          break
        default:
          ElMessage.error(error.message || '请求失败')
      }
    } else {
      ElMessage.error('网络错误，请检查网络连接')
    }
    
    return Promise.reject(error)
  }
)

export default service
```

---

### 3.2 布局模块

#### 3.2.1 主布局组件

```vue
<!-- layout/index.vue -->
<template>
  <div class="app-wrapper" :class="classObj">
    <div v-if="device === 'mobile' && sidebar.opened" class="drawer-bg" @click="handleClickOutside" />
    
    <el-container class="main-container">
      <!-- 侧边栏 -->
      <el-aside :width="sidebarWidth">
        <Sidebar />
      </el-aside>
      
      <el-container>
        <!-- 顶部导航 -->
        <el-header class="header">
          <Navbar />
        </el-header>
        
        <!-- 标签页 -->
        <div v-if="tagsView" class="tags-view-container">
          <TagsView />
        </div>
        
        <!-- 主内容区 -->
        <el-main class="main-content">
          <router-view v-slot="{ Component, route }">
            <transition name="fade-transform" mode="out-in">
              <keep-alive :include="cachedViews">
                <component :is="Component" :key="route.path" />
              </keep-alive>
            </transition>
          </router-view>
        </el-main>
      </el-container>
    </el-container>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useAppStore } from '@/stores/app'
import Sidebar from './components/Sidebar.vue'
import Navbar from './components/Navbar.vue'
import TagsView from './components/TagsView.vue'

const appStore = useAppStore()

const sidebar = computed(() => appStore.sidebar)
const device = computed(() => appStore.device)
const sidebarWidth = computed(() => sidebar.value.opened ? '210px' : '54px')
const classObj = computed(() => ({
  hideSidebar: !sidebar.value.opened,
  openSidebar: sidebar.value.opened,
  mobile: device.value === 'mobile'
}))

const handleClickOutside = () => {
  appStore.closeSidebar()
}
</script>

<style scoped lang="scss">
.app-wrapper {
  position: relative;
  height: 100%;
  width: 100%;
  
  &.openSidebar {
    position: fixed;
    top: 0;
  }
}

.drawer-bg {
  background: #000;
  opacity: 0.3;
  width: 100%;
  top: 0;
  height: 100%;
  position: absolute;
  z-index: 999;
}

.main-container {
  height: 100%;
  position: relative;
}

.el-aside {
  transition: width 0.28s;
  background-color: #304156;
  overflow: visible;
}

.header {
  background: #fff;
  box-shadow: 0 1px 4px rgba(0, 21, 41, 0.08);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 20px;
}

.main-content {
  background: #f0f2f5;
  padding: 20px;
  overflow-y: auto;
}

.tags-view-container {
  background: #fff;
  box-shadow: 0 1px 4px rgba(0, 21, 41, 0.08);
}
</style>
```

#### 3.2.2 侧边栏组件

```vue
<!-- layout/components/Sidebar.vue -->
<template>
  <div class="sidebar-container">
    <el-scrollbar>
      <el-menu
        :default-active="activeMenu"
        :collapse="!sidebar.opened"
        :background-color="'#304156'"
        :text-color="'#bfcbd9'"
        :active-text-color="'#409EFF'"
        :unique-opened="false"
        :collapse-transition="false"
        mode="vertical"
      >
        <SidebarItem
          v-for="menu in menus"
          :key="menu.menuId"
          :menu="menu"
          :base-path="menu.menuUrl || '/'"
        />
      </el-menu>
    </el-scrollbar>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRoute } from 'vue-router'
import { useAppStore } from '@/stores/app'
import { useMenuStore } from '@/stores/menu'
import SidebarItem from './SidebarItem.vue'

const route = useRoute()
const appStore = useAppStore()
const menuStore = useMenuStore()

const sidebar = computed(() => appStore.sidebar)
const menus = computed(() => menuStore.menus)
const activeMenu = computed(() => route.path)
</script>

<style scoped lang="scss">
.sidebar-container {
  height: 100%;
  background: #304156;
  
  .el-menu {
    border-right: none;
  }
}
</style>
```

#### 3.2.3 侧边栏菜单项组件

```vue
<!-- layout/components/SidebarItem.vue -->
<template>
  <div v-if="!menu.visible">
    <!-- 隐藏菜单 -->
  </div>
  
  <el-sub-menu v-else-if="hasChildren && isDirectory" :index="menu.menuId">
    <template #title>
      <el-icon v-if="menu.menuIcon">
        <component :is="menu.menuIcon" />
      </el-icon>
      <span>{{ menu.menuName }}</span>
    </template>
    
    <SidebarItem
      v-for="child in menu.children"
      :key="child.menuId"
      :menu="child"
      :base-path="resolvePath(child.menuUrl || '')"
    />
  </el-sub-menu>
  
  <el-menu-item v-else-if="isMenu" :index="resolvePath(menu.menuUrl || '')">
    <el-icon v-if="menu.menuIcon">
      <component :is="menu.menuIcon" />
    </el-icon>
    <span>{{ menu.menuName }}</span>
  </el-menu-item>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { Menu } from '@/types/menu'

interface Props {
  menu: Menu
  basePath: string
}

const props = defineProps<Props>()

const hasChildren = computed(() => {
  return props.menu.children && props.menu.children.length > 0
})

const isDirectory = computed(() => {
  return props.menu.menuType === 1
})

const isMenu = computed(() => {
  return props.menu.menuType === 2
})

const resolvePath = (path: string) => {
  if (path.startsWith('http://') || path.startsWith('https://')) {
    return path
  }
  if (path.startsWith('/')) {
    return path
  }
  return `${props.basePath}/${path}`
}
</script>
```

---

### 3.3 通用组件

#### 3.3.1 分页组件

```vue
<!-- components/common/Pagination.vue -->
<template>
  <el-pagination
    v-model:current-page="currentPage"
    v-model:page-size="pageSize"
    :page-sizes="[10, 20, 50, 100]"
    :total="total"
    layout="total, sizes, prev, pager, next, jumper"
    @size-change="handleSizeChange"
    @current-change="handleCurrentChange"
  />
</template>

<script setup lang="ts">
import { computed } from 'vue'

interface Props {
  modelValue: {
    page: number
    size: number
  }
  total: number
}

const props = defineProps<Props>()

const emit = defineEmits<{
  'update:modelValue': [value: { page: number; size: number }]
  'change': [value: { page: number; size: number }]
}>()

const currentPage = computed({
  get: () => props.modelValue.page,
  set: (val) => emit('update:modelValue', { ...props.modelValue, page: val })
})

const pageSize = computed({
  get: () => props.modelValue.size,
  set: (val) => emit('update:modelValue', { ...props.modelValue, size: val })
})

const handleSizeChange = () => {
  emit('change', { page: 1, size: pageSize.value })
}

const handleCurrentChange = () => {
  emit('change', { page: currentPage.value, size: pageSize.value })
}
</script>
```

#### 3.3.2 搜索表单组件

```vue
<!-- components/common/SearchForm.vue -->
<template>
  <el-card class="search-form" shadow="never">
    <el-form :model="formData" :inline="true" label-width="80px">
      <el-row :gutter="20">
        <el-col
          v-for="(item, index) in visibleFields"
          :key="item.prop"
          :span="span"
          :style="{ display: index >= visibleCount && !isExpand ? 'none' : '' }"
        >
          <el-form-item :label="item.label">
            <el-input
              v-if="item.type === 'input'"
              v-model="formData[item.prop]"
              :placeholder="item.placeholder"
              clearable
              @keyup.enter="handleSearch"
            />
            
            <el-select
              v-else-if="item.type === 'select'"
              v-model="formData[item.prop]"
              :placeholder="item.placeholder"
              clearable
              @change="handleSearch"
            >
              <el-option
                v-for="option in item.options"
                :key="option.value"
                :label="option.label"
                :value="option.value"
              />
            </el-select>
            
            <el-date-picker
              v-else-if="item.type === 'date'"
              v-model="formData[item.prop]"
              type="date"
              :placeholder="item.placeholder"
              value-format="YYYY-MM-DD"
              @change="handleSearch"
            />
          </el-form-item>
        </el-col>
        
        <el-col :span="span">
          <el-form-item>
            <el-button type="primary" @click="handleSearch">
              <el-icon><Search /></el-icon>
              查询
            </el-button>
            <el-button @click="handleReset">
              <el-icon><Refresh /></el-icon>
              重置
            </el-button>
            <el-link
              v-if="hasMore"
              type="primary"
              :underline="false"
              @click="toggleExpand"
            >
              {{ isExpand ? '收起' : '展开' }}
              <el-icon><ArrowUp v-if="isExpand" /><ArrowDown v-else /></el-icon>
            </el-link>
          </el-form-item>
        </el-col>
      </el-row>
    </el-form>
  </el-card>
</template>

<script setup lang="ts">
import { computed, ref } from 'vue'
import { Search, Refresh, ArrowUp, ArrowDown } from '@element-plus/icons-vue'

interface SearchField {
  prop: string
  label: string
  type: 'input' | 'select' | 'date'
  placeholder?: string
  options?: Array<{ label: string; value: any }>
}

interface Props {
  modelValue: Record<string, any>
  fields: SearchField[]
  span?: number
  maxVisible?: number
}

const props = withDefaults(defineProps<Props>(), {
  span: 6,
  maxVisible: 4
})

const emit = defineEmits<{
  'update:modelValue': [value: Record<string, any>]
  'search': [value: Record<string, any>]
  'reset': []
}>()

const isExpand = ref(false)
const formData = computed({
  get: () => props.modelValue,
  set: (val) => emit('update:modelValue', val)
})

const visibleCount = computed(() => props.maxVisible)
const hasMore = computed(() => props.fields.length > props.maxVisible)
const visibleFields = computed(() => props.fields)

const handleSearch = () => {
  emit('search', formData.value)
}

const handleReset = () => {
  formData.value = {}
  emit('reset')
  emit('search', {})
}

const toggleExpand = () => {
  isExpand.value = !isExpand.value
}
</script>

<style scoped lang="scss">
.search-form {
  margin-bottom: 16px;
  
  .el-form-item {
    margin-bottom: 16px;
  }
}
</style>
```

#### 3.3.3 表格操作栏组件

```vue
<!-- components/common/TableOperator.vue -->
<template>
  <div class="table-operator">
    <div class="operator-left">
      <slot name="left">
        <el-button
          v-if="showAdd"
          type="primary"
          @click="$emit('add')"
        >
          <el-icon><Plus /></el-icon>
          新增
        </el-button>
        
        <el-button
          v-if="showBatchDelete"
          type="danger"
          :disabled="!selectedRows || selectedRows.length === 0"
          @click="$emit('batchDelete')"
        >
          <el-icon><Delete /></el-icon>
          批量删除
        </el-button>
        
        <el-button
          v-if="showExport"
          @click="$emit('export')"
        >
          <el-icon><Download /></el-icon>
          导出
        </el-button>
      </slot>
    </div>
    
    <div class="operator-right">
      <slot name="right">
        <el-button
          v-if="showRefresh"
          @click="$emit('refresh')"
        >
          <el-icon><Refresh /></el-icon>
          刷新
        </el-button>
      </slot>
    </div>
  </div>
</template>

<script setup lang="ts">
import { Plus, Delete, Download, Refresh } from '@element-plus/icons-vue'

interface Props {
  showAdd?: boolean
  showBatchDelete?: boolean
  showExport?: boolean
  showRefresh?: boolean
  selectedRows?: any[]
}

withDefaults(defineProps<Props>(), {
  showAdd: true,
  showBatchDelete: false,
  showExport: false,
  showRefresh: true
})

defineEmits<{
  add: []
  batchDelete: []
  export: []
  refresh: []
}>()
</script>

<style scoped lang="scss">
.table-operator {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
  
  .operator-left,
  .operator-right {
    display: flex;
    gap: 10px;
  }
}
</style>
```

---

## 四、页面模块设计

### 4.1 登录页

```vue
<!-- views/login/index.vue -->
<template>
  <div class="login-container">
    <el-card class="login-card">
      <div class="login-header">
        <h2>客户运营管理后台系统</h2>
      </div>
      
      <el-form
        ref="loginFormRef"
        :model="loginForm"
        :rules="loginRules"
        class="login-form"
      >
        <el-form-item prop="userCode">
          <el-input
            v-model="loginForm.userCode"
          placeholder="请输入工号"
            prefix-icon="User"
            size="large"
          />
        </el-form-item>
        
        <el-form-item prop="password">
          <el-input
            v-model="loginForm.password"
            type="password"
            placeholder="请输入密码"
            prefix-icon="Lock"
            size="large"
            show-password
            @keyup.enter="handleLogin"
          />
        </el-form-item>
        
        <el-button
          type="primary"
          size="large"
          :loading="loading"
          class="login-button"
          @click="handleLogin"
        >
          登录
        </el-button>
      </el-form>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import type { FormInstance, FormRules } from 'element-plus'
import { useUserStore } from '@/stores/user'

const router = useRouter()
const route = useRoute()
const userStore = useUserStore()

const loginFormRef = ref<FormInstance>()
const loading = ref(false)

const loginForm = reactive({
  userCode: '',
  password: ''
})

const loginRules: FormRules = {
  userCode: [
    { required: true, message: '请输入工号', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码长度不能小于 6 位', trigger: 'blur' }
  ]
}

const handleLogin = async () => {
  if (!loginFormRef.value) return
  
  await loginFormRef.value.validate(async (valid) => {
    if (valid) {
      loading.value = true
      
      try {
        await userStore.login(loginForm)
        
        ElMessage.success('登录成功')
        
        const redirect = route.query.redirect as string
        router.push(redirect || '/')
      } catch (error) {
        console.error('登录失败:', error)
      } finally {
        loading.value = false
      }
    }
  })
}
</script>

<style scoped lang="scss">
.login-container {
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.login-card {
  width: 400px;
  padding: 20px;
}

.login-header {
  text-align: center;
  margin-bottom: 30px;
  
  h2 {
    color: #333;
    font-size: 24px;
    font-weight: 600;
  }
}

.login-form {
  .el-form-item {
    margin-bottom: 24px;
  }
}

.login-button {
  width: 100%;
  margin-top: 10px;
}
</style>
```

### 4.2 机构管理页

```vue
<!-- views/system/org/index.vue -->
<template>
  <div class="org-page">
    <!-- 搜索表单 -->
    <SearchForm
      v-model="searchForm"
      :fields="searchFields"
      @search="handleSearch"
      @reset="handleReset"
    />
    
    <!-- 操作栏 -->
    <TableOperator
      show-add
      show-refresh
      @add="handleAdd"
      @refresh="getList"
    >
      <template #left>
        <el-button
          v-permission="['sys:org:delete']"
          type="danger"
          :disabled="!selectedRows || selectedRows.length === 0"
          @click="handleBatchDelete"
        >
          <el-icon><Delete /></el-icon>
          批量删除
        </el-button>
      </template>
    </TableOperator>
    
    <!-- 表格 -->
    <el-card shadow="never">
      <el-table
        v-loading="loading"
        :data="tableData"
        row-key="orgId"
        :tree-props="{ children: 'children', hasChildren: 'hasChildren' }"
        :default-expand-all="true"
        @selection-change="handleSelectionChange"
      >
        <el-table-column type="selection" width="55" />
        <el-table-column type="index" label="序号" width="60" />
        <el-table-column prop="orgCode" label="机构代码" min-width="120" />
        <el-table-column prop="orgName" label="机构名称" min-width="200" />
        <el-table-column prop="orgLevel" label="机构级别" width="100">
          <template #default="{ row }">
            <el-tag :type="getOrgLevelType(row.orgLevel)">
              {{ getOrgLevelLabel(row.orgLevel) }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="orgStatus" label="状态" width="80">
          <template #default="{ row }">
            <el-tag :type="row.orgStatus === 1 ? 'success' : 'danger'">
              {{ row.orgStatus === 1 ? '启用' : '停用' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="sortOrder" label="排序" width="80" />
        <el-table-column label="操作" width="280" fixed="right">
          <template #default="{ row }">
            <el-button
              v-permission="['sys:org:edit']"
              type="primary"
              link
              @click="handleEdit(row)"
            >
              编辑
            </el-button>
            <el-button
              v-permission="['sys:org:add']"
              type="primary"
              link
              @click="handleAddChild(row)"
            >
              新增子机构
            </el-button>
            <el-button
              v-permission="['sys:org:delete']"
              type="danger"
              link
              @click="handleDelete(row)"
            >
              删除
            </el-button>
          </template>
        </el-table-column>
      </el-table>
    </el-card>
    
    <!-- 表单弹窗 -->
    <OrgForm
      v-model:visible="formVisible"
      :form-data="formData"
      :org-tree="orgTree"
      @submit="handleSubmit"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { Delete } from '@element-plus/icons-vue'
import { getOrgList, deleteOrg, getOrgTree } from '@/api/system/org'
import type { Org } from '@/types/org'
import SearchForm from '@/components/common/SearchForm.vue'
import TableOperator from '@/components/common/TableOperator.vue'
import OrgForm from './components/OrgForm.vue'

const loading = ref(false)
const tableData = ref<Org[]>([])
const orgTree = ref<Org[]>([])
const selectedRows = ref<Org[]>([])
const formVisible = ref(false)
const formData = ref<Partial<Org>>({})

const searchForm = reactive({
  orgName: '',
  orgStatus: undefined
})

const searchFields = [
  {
    prop: 'orgName',
    label: '机构名称',
    type: 'input' as const,
    placeholder: '请输入机构名称'
  },
  {
    prop: 'orgStatus',
    label: '状态',
    type: 'select' as const,
    placeholder: '请选择状态',
    options: [
      { label: '启用', value: 1 },
      { label: '停用', value: 0 }
    ]
  }
]

onMounted(() => {
  getOrgTreeData()
})

const getOrgTreeData = async () => {
  try {
    const res = await getOrgTree()
    orgTree.value = res.data
    tableData.value = res.data
  } catch (error) {
    console.error('获取机构树失败:', error)
  }
}

const getList = async () => {
  loading.value = true
  try {
    const res = await getOrgList(searchForm)
    tableData.value = res.data
  } catch (error) {
    console.error('获取机构列表失败:', error)
  } finally {
    loading.value = false
  }
}

const handleSearch = () => {
  getList()
}

const handleReset = () => {
  getList()
}

const handleSelectionChange = (rows: Org[]) => {
  selectedRows.value = rows
}

const handleAdd = () => {
  formData.value = {}
  formVisible.value = true
}

const handleEdit = (row: Org) => {
  formData.value = { ...row }
  formVisible.value = true
}

const handleAddChild = (row: Org) => {
  formData.value = {
    parentOrgId: row.orgId,
    orgLevel: row.orgLevel + 1
  }
  formVisible.value = true
}

const handleDelete = (row: Org) => {
  ElMessageBox.confirm('确认删除该机构吗？', '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    try {
      await deleteOrg(row.orgId)
      ElMessage.success('删除成功')
      getOrgTreeData()
    } catch (error) {
      console.error('删除失败:', error)
    }
  })
}

const handleBatchDelete = () => {
  ElMessageBox.confirm(`确认删除选中的 ${selectedRows.value.length} 个机构吗？`, '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    try {
      const ids = selectedRows.value.map(row => row.orgId)
      await Promise.all(ids.map(id => deleteOrg(id)))
      ElMessage.success('批量删除成功')
      getOrgTreeData()
    } catch (error) {
      console.error('批量删除失败:', error)
    }
  })
}

const handleSubmit = async () => {
  try {
    await getList()
    formVisible.value = false
    ElMessage.success('操作成功')
  } catch (error) {
    console.error('操作失败:', error)
  }
}

const getOrgLevelLabel = (level: number) => {
  const map: Record<number, string> = {
    0: '总行',
    1: '分行',
    2: '支行',
    3: '分理处'
  }
  return map[level] || ''
}

const getOrgLevelType = (level: number) => {
  const map: Record<number, 'success' | 'primary' | 'warning' | 'danger'> = {
    0: 'success',
    1: 'primary',
    2: 'warning',
    3: 'danger'
  }
  return map[level] || ''
}
</script>

<style scoped lang="scss">
.org-page {
  padding: 0;
}
</style>
```

### 4.3 机构表单组件

```vue
<!-- views/system/org/components/OrgForm.vue -->
<template>
  <el-dialog
    :model-value="visible"
    :title="formData.orgId ? '编辑机构' : '新增机构'"
    width="600px"
    @close="handleClose"
  >
    <el-form
      ref="formRef"
      :model="formData"
      :rules="formRules"
      label-width="100px"
    >
      <el-form-item label="上级机构" prop="parentOrgId">
        <el-tree-select
          v-model="formData.parentOrgId"
          :data="orgTree"
          :props="{ label: 'orgName', value: 'orgId', children: 'children' }"
          placeholder="请选择上级机构"
          check-strictly
          :default-expand-all="true"
        />
      </el-form-item>
      
      <el-form-item label="机构代码" prop="orgCode">
        <el-input
          v-model="formData.orgCode"
          placeholder="请输入机构代码"
          :disabled="!!formData.orgId"
        />
      </el-form-item>
      
      <el-form-item label="机构名称" prop="orgName">
        <el-input
          v-model="formData.orgName"
          placeholder="请输入机构名称"
        />
      </el-form-item>
      
      <el-form-item label="机构级别" prop="orgLevel">
        <el-select
          v-model="formData.orgLevel"
          placeholder="请选择机构级别"
          :disabled="!!formData.orgId"
        >
          <el-option label="总行" :value="0" />
          <el-option label="分行" :value="1" />
          <el-option label="支行" :value="2" />
          <el-option label="分理处" :value="3" />
        </el-select>
      </el-form-item>
      
      <el-form-item label="状态" prop="orgStatus">
        <el-radio-group v-model="formData.orgStatus">
          <el-radio :label="1">启用</el-radio>
          <el-radio :label="0">停用</el-radio>
        </el-radio-group>
      </el-form-item>
      
      <el-form-item label="排序号" prop="sortOrder">
        <el-input-number
          v-model="formData.sortOrder"
          :min="0"
          :step="1"
        />
      </el-form-item>
      
      <el-form-item label="备注" prop="remark">
        <el-input
          v-model="formData.remark"
          type="textarea"
          :rows="3"
          placeholder="请输入备注"
        />
      </el-form-item>
    </el-form>
    
    <template #footer>
      <el-button @click="handleClose">取消</el-button>
      <el-button type="primary" :loading="submitLoading" @click="handleSubmit">
        确定
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, reactive, computed, watch } from 'vue'
import type { FormInstance, FormRules } from 'element-plus'
import { addOrg, updateOrg } from '@/api/system/org'
import type { Org } from '@/types/org'

interface Props {
  visible: boolean
  formData: Partial<Org>
  orgTree: Org[]
}

const props = withDefaults(defineProps<Props>(), {
  visible: false,
  formData: () => ({}),
  orgTree: () => []
})

const emit = defineEmits<{
  'update:visible': [value: boolean]
  'submit': []
}>()

const formRef = ref<FormInstance>()
const submitLoading = ref(false)

const localFormData = reactive<Partial<Org>>({
  parentOrgId: undefined,
  orgCode: '',
  orgName: '',
  orgLevel: 1,
  orgStatus: 1,
  sortOrder: 0,
  remark: ''
})

watch(() => props.formData, (val) => {
  if (val) {
    Object.assign(localFormData, val)
  }
}, { immediate: true, deep: true })

const formRules: FormRules = {
  parentOrgId: [
    { required: true, message: '请选择上级机构', trigger: 'change' }
  ],
  orgCode: [
    { required: true, message: '请输入机构代码', trigger: 'blur' }
  ],
  orgName: [
    { required: true, message: '请输入机构名称', trigger: 'blur' }
  ],
  orgLevel: [
    { required: true, message: '请选择机构级别', trigger: 'change' }
  ],
  orgStatus: [
    { required: true, message: '请选择状态', trigger: 'change' }
  ]
}

const handleClose = () => {
  emit('update:visible', false)
  formRef.value?.resetFields()
}

const handleSubmit = async () => {
  if (!formRef.value) return
  
  await formRef.value.validate(async (valid) => {
    if (valid) {
      submitLoading.value = true
      
      try {
        if (localFormData.orgId) {
          await updateOrg(localFormData)
        } else {
          await addOrg(localFormData)
        }
        
        emit('submit')
        handleClose()
      } catch (error) {
        console.error('提交失败:', error)
      } finally {
        submitLoading.value = false
      }
    }
  })
}
</script>
```

---

## 五、API 接口封装

### 5.1 Axios 配置

```typescript
// utils/request.ts
export const request = {
  get: <T>(url: string, params?: any) => service.get<T>(url, { params }),
  post: <T>(url: string, data?: any) => service.post<T>(url, data),
  put: <T>(url: string, data?: any) => service.put<T>(url, data),
  delete: <T>(url: string, params?: any) => service.delete<T>(url, { params })
}
```

### 5.2 机构管理接口

```typescript
// api/system/org.ts
import { request } from '@/utils/request'
import type { Org, OrgQuery, OrgCreate, OrgUpdate } from '@/types/org'

/**
 * 获取机构树
 */
export const getOrgTree = () => request.get<Org[]>('/system/org/tree')

/**
 * 获取机构列表
 */
export const getOrgList = (params: OrgQuery) => request.get<Org[]>('/system/org/list', params)

/**
 * 获取机构详情
 */
export const getOrgDetail = (orgId: string) => request.get<Org>(`/system/org/${orgId}`)

/**
 * 新增机构
 */
export const addOrg = (data: OrgCreate) => request.post('/system/org', data)

/**
 * 编辑机构
 */
export const updateOrg = (data: OrgUpdate) => request.put(`/system/org/${data.orgId}`, data)

/**
 * 删除机构
 */
export const deleteOrg = (orgId: string) => request.delete(`/system/org/${orgId}`)
```

### 5.3 用户管理接口

```typescript
// api/system/user.ts
import { request } from '@/utils/request'
import type { User, UserQuery, UserCreate, UserUpdate } from '@/types/user'

/**
 * 登录
 */
export const login = (data: { userCode: string; password: string }) =>
  request.post<{ token: string }>('/system/user/login', data)

/**
 * 获取用户信息
 */
export const getUserInfo = () => request.get<{ userInfo: User }>('/system/user/info')

/**
 * 获取用户列表
 */
export const getUserList = (params: UserQuery) =>
  request.get<{ list: User[]; total: number }>('/system/user/list', params)

/**
 * 获取用户详情
 */
export const getUserDetail = (userId: string) =>
  request.get<User>(`/system/user/${userId}`)

/**
 * 新增用户
 */
export const addUser = (data: UserCreate) =>
  request.post('/system/user', data)

/**
 * 编辑用户
 */
export const updateUser = (data: UserUpdate) =>
  request.put(`/system/user/${data.userId}`, data)

/**
 * 删除用户
 */
export const deleteUser = (userId: string) =>
  request.delete(`/system/user/${userId}`)

/**
 * 重置密码
 */
export const resetPassword = (userId: string) =>
  request.put(`/system/user/${userId}/password/reset`)

/**
 * 登出
 */
export const logout = () => request.post('/system/user/logout')
```

---

## 六、环境变量配置

```env
# .env.development
VITE_API_BASE_URL=http://localhost:8080/api
VITE_APP_TITLE=客户运营管理后台系统 - 开发环境

# .env.test
VITE_API_BASE_URL=http://test-server:8080/api
VITE_APP_TITLE=客户运营管理后台系统 - 测试环境

# .env.production
VITE_API_BASE_URL=https://api.example.com
VITE_APP_TITLE=客户运营管理后台系统
```

---

## 七、开发计划

### 7.1 时间安排

| 模块 | 开始日期 | 结束日期 | 工作日 | 负责人 |
|------|----------|----------|--------|--------|
| 权限控制 | 2026-03-08 | 2026-03-09 | 2 | 前端开发 |
| 布局框架 | 2026-03-08 | 2026-03-08 | 1 | 前端开发 |
| 机构管理 | 2026-03-10 | 2026-03-11 | 2 | 前端开发 |
| 用户管理 | 2026-03-12 | 2026-03-13 | 2 | 前端开发 |
| 菜单管理 | 2026-03-14 | 2026-03-15 | 2 | 前端开发 |
| 角色管理 | 2026-03-16 | 2026-03-17 | 2 | 前端开发 |
| 集成测试 | 2026-03-18 | 2026-03-19 | 2 | 前端开发 |

### 7.2 开发顺序

1. **权限控制基础设施**（路由守卫、权限指令、Store）
2. **布局框架**（Sidebar、Navbar、TagsView）
3. **通用组件**（Pagination、SearchForm、TableOperator）
4. **机构管理**（列表、表单、树形结构）
5. **用户管理**（列表、表单、角色分配）
6. **菜单管理**（列表、表单、树形表格）
7. **角色管理**（列表、表单、权限分配）
8. **联调测试**

---

## 八、注意事项

### 8.1 代码规范

1. 使用 `<script setup lang="ts">` 语法
2. 组件名使用 PascalCase 命名
3. 变量名使用 camelCase 命名
4. 常量名使用 UPPER_SNAKE_CASE 命名
5. 文件命名使用 kebab-case

### 8.2 类型定义

1. 所有 API 接口必须有明确的类型定义
2. 使用 TypeScript 严格模式
3. 避免使用 `any` 类型

### 8.3 组件设计

1. 组件职责单一
2. 使用 props 和 emits 进行父子组件通信
3. 使用 composables 抽取可复用逻辑
4. 使用 slots 提高组件灵活性

### 8.4 性能优化

1. 使用 `keep-alive` 缓存页面组件
2. 使用虚拟滚动优化长列表
3. 使用懒加载优化首屏
4. 使用防抖节流优化频繁操作

### 8.5 安全注意

1. Token 存储在 localStorage
2. 所有请求必须携带 Token
3. 敏感操作需要二次确认
4. 表单输入需要 XSS 过滤

---

## 九、交付物

1. [ ] 完整的 Vue3 + TypeScript 工程
2. [ ] 权限控制模块（路由守卫、权限指令）
3. [ ] 布局组件（Sidebar、Navbar、TagsView）
4. [ ] 通用组件（Pagination、SearchForm、TableOperator）
5. [ ] 机构管理页面（列表、表单）
6. [ ] 用户管理页面（列表、表单）
7. [ ] 菜单管理页面（列表、表单）
8. [ ] 角色管理页面（列表、表单）
9. [ ] API 接口封装
10. [ ] 环境变量配置

---

## 十、版本记录

| 版本 | 日期 | 修改内容 | 编制人 |
|------|------|----------|--------|
| v1.0.0 | 2026-03-07 | 初始版本 | 前端开发 Agent |
