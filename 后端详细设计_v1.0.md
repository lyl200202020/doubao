# 客户运营管理后台系统 - 后端详细设计文档

## 1 技术架构设计

### 1.1 技术栈选型

| 层级 | 技术 | 版本 |
|------|------|------|
| 语言 | JDK | 17 |
| 框架 | Spring Boot | 3.3.x |
| 微服务 | Spring Cloud Alibaba | 2023.0.1.2 |
| RPC框架 | Dubbo | 3.2.14 |
| 注册/配置 | Nacos | 2.2.x |
| ORM | MyBatis-Plus | 3.5.x |
| 数据库 | MySQL | 8.0.x |
| 缓存 | Redis | 7.0.x |
| 熔断限流 | Sentinel | 1.8.x |
| 安全框架 | Spring Security | 6.x |
| 国密算法 | BouncyCastle | 1.77 |
| 文档 | SpringDoc OpenAPI | 2.x |

### 1.2 架构模式

采用**DDD领域驱动设计 + 微服务架构**：

```
┌─────────────────────────────────────────────────────────────────┐
│                      客户端 (Vue3)                               │
└─────────────────────────────┬───────────────────────────────────┘
                              │ HTTP
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   admin-platform (运营平台)                      │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                    Interface层                          │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │   │
│   │   │  Controller │  │    DTO/VO   │  │   拦截器    │    │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘    │   │
│   └────────────────────────┬────────────────────────────────┘   │
│                            ▼                                     │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                   Application层                        │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │   │
│   │   │ 应用服务    │  │   用例      │  │  事件发布   │    │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘    │   │
│   └────────────────────────┬────────────────────────────────┘   │
│                            ▼                                     │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                    Domain层                             │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │   │
│   │   │   实体      │  │  领域服务   │  │  领域事件   │    │   │
│   │   │  聚合根    │  │  值对象     │  │  仓储接口   │    │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘    │   │
│   └────────────────────────┬────────────────────────────────┘   │
│                            ▼                                     │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                 Infrastructure层                        │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │   │
│   │   │    DAO     │  │ Dubbo接口   │  │   Redis    │    │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘    │   │
│   └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────┘
                              │ Dubbo RPC
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    crm-service (客户管理服务)                    │
│                         (同上架构)                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2 项目目录结构

### 2.1 父POM结构

```
customer-admin/
├── customer-admin-parent/           # 父POM，统一版本管理
│   └── pom.xml
├── customer-admin-common/           # 公共模块
│   ├── common-core/                # 核心工具类
│   │   ├── pom.xml
│   │   └── src/main/java/.../common/
│   │       ├── constant/           # 常量定义
│   │       ├── exception/          # 异常定义
│   │       ├── enums/              # 枚举
│   │       ├── result/             # 统一返回
│   │       └── util/               # 工具类
│   │           ├── SM2Util.java    # 国密SM2
│   │           ├── SM3Util.java    # 国密SM3
│   │           └── SM4Util.java    # 国密SM4
│   ├── common-security/            # 安全组件
│   │   ├── pom.xml
│   │   └── src/main/java/.../security/
│   │       ├── config/             # 安全配置
│   │       ├── filter/             # 过滤器
│   │       ├── handler/             # 处理器
│   │       └── jwt/                # JWT工具
│   ├── common-redis/               # Redis组件
│   │   ├── pom.xml
│   │   └── src/main/java/.../redis/
│   │       ├── config/             # Redis配置
│   │       └── cache/              # 缓存工具
│   └── common-dubbo/               # Dubbo接口定义
│       ├── pom.xml
│       └── src/main/java/.../dubbo/
│           ├── api/                 # API接口
│           └── model/               # 传输对象
│
├── customer-admin-platform/         # 运营平台服务
│   ├── platform-biz/              # 业务实现
│   │   ├── pom.xml
│   │   └── src/main/java/.../platform/
│   │       ├── sys/               # 系统管理领域
│   │       │   ├── domain/         # 领域层
│   │       │   │   ├── entity/     # 实体
│   │       │   │   ├── service/    # 领域服务
│   │       │   │   ├── repository/ # 仓储接口
│   │       │   │   └── event/      # 领域事件
│   │       │   ├── application/    # 应用层
│   │       │   │   ├── service/    # 应用服务
│   │       │   │   ├── command/    # 命令对象
│   │       │   │   └── query/     # 查询对象
│   │       │   ├── infrastructure/# 基础设施层
│   │       │   │   ├── dao/        # DAO实现
│   │       │   │   └── repository/ # 仓储实现
│   │       │   └── interface/      # 接口层
│   │       │       ├── controller/ # 控制器
│   │       │       ├── dto/         # DTO
│   │       │       └── convert/     # 转换器
│   │       │
│   │       └── auth/               # 授权管理领域（同上结构）
│   │           ├── domain/
│   │           ├── application/
│   │           ├── infrastructure/
│   │           └── interface/
│   │
│   └── platform-start/             # 启动模块
│       ├── pom.xml
│       └── src/main/java/.../
│           ├── PlatformApplication.java
│           └── config/
│
├── customer-admin-crm/             # 客户管理服务
│   ├── crm-biz/                   # 业务实现
│   │   ├── pom.xml
│   │   └── src/main/java/.../crm/
│   │       ├── customer/           # 客户管理领域
│   │       │   ├── domain/
│   │       │   ├── application/
│   │       │   ├── infrastructure/
│   │       │   └── interface/
│   │       └── contract/           # 签约管理领域
│   │           ├── domain/
│   │           ├── application/
│   │           ├── infrastructure/
│   │           └── interface/
│   │
│   └── crm-start/                 # 启动模块
│       ├── pom.xml
│       └── src/main/java/.../
│           └── CrmApplication.java
│
└── sql/                           # SQL脚本
    ├── init.sql                   # 初始化数据
    └── schema.sql                 # 建表脚本
```

### 2.2 领域目录结构

```
├── domain/                        # 领域层（核心业务逻辑）
│   ├── entity/                    # 实体类
│   │   ├── SysUser.java          # 用户实体
│   │   ├── SysRole.java          # 角色实体
│   │   ├── SysMenu.java          # 菜单实体
│   │   └── SysOrg.java           # 机构实体
│   ├── valueobject/              # 值对象
│   │   ├── AuthLevel.java        # 授权级别
│   │   └── OrgLevel.java        # 机构级别
│   ├── service/                  # 领域服务
│   │   ├── UserDomainService.java
│   │   └── AuthDomainService.java
│   ├── repository/               # 仓储接口
│   │   ├── UserRepository.java
│   │   ├── RoleRepository.java
│   │   └── MenuRepository.java
│   └── event/                    # 领域事件
│       ├── UserCreatedEvent.java
│       └── AuthSubmittedEvent.java
│
├── application/                   # 应用层（用例编排）
│   ├── service/                  # 应用服务
│   │   ├── UserApplicationService.java
│   │   └── AuthApplicationService.java
│   ├── command/                  # 命令对象（写操作）
│   │   ├── CreateUserCommand.java
│   │   └── SubmitAuthCommand.java
│   └── query/                    # 查询对象（读操作）
│       ├── UserQuery.java
│       └── AuthTaskQuery.java
│
├── infrastructure/               # 基础设施层（技术实现）
│   ├── dao/                      # DAO接口
│   │   ├── SysUserDao.java
│   │   └── SysRoleDao.java
│   ├── dao/mapper/               # MyBatis Mapper
│   │   ├── SysUserMapper.java
│   │   └── SysRoleMapper.xml
│   └── repository/               # 仓储实现
│       ├── UserRepositoryImpl.java
│       └── RoleRepositoryImpl.java
│
└── interface/                   # 接口层（对外API）
    ├── controller/               # REST控制器
    │   ├── SysUserController.java
    │   └── AuthController.java
    ├── dto/                      # 数据传输对象
    │   ├── UserDTO.java
    │   └── AuthTaskDTO.java
    ├── vo/                       # 视图对象
    │   ├── Result.java           # 统一返回
    │   └── PageVO.java           # 分页返回
    └── convert/                  # 对象转换器
        └── UserConvert.java
```

---

## 3 数据库设计

### 3.1 数据库划分

| 数据库名 | 所属服务 | 说明 |
|----------|----------|------|
| admin_platform | admin-platform | 系统管理、授权管理 |
| admin_crm | crm-service | 客户管理、签约管理 |

### 3.2 核心表结构

#### 3.2.1 系统管理表（admin_platform）

```sql
-- 用户表
CREATE TABLE sys_user (
    user_id VARCHAR(32) PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(128) NOT NULL COMMENT '密码(SM3加密)',
    real_name VARCHAR(50) NOT NULL COMMENT '真实姓名',
    org_id VARCHAR(32) NOT NULL COMMENT '所属机构ID',
    auth_level TINYINT NOT NULL DEFAULT 1 COMMENT '授权级别(1-16)',
    user_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0-禁用,1-启用',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_org (org_id),
    INDEX idx_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 角色表
CREATE TABLE sys_role (
    role_id VARCHAR(32) PRIMARY KEY,
    role_code VARCHAR(50) NOT NULL UNIQUE COMMENT '角色代码',
    role_name VARCHAR(100) NOT NULL COMMENT '角色名称',
    role_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0-禁用,1-启用',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_code (role_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 用户-角色关联表
CREATE TABLE sys_user_role (
    user_id VARCHAR(32) NOT NULL,
    role_id VARCHAR(32) NOT NULL,
    PRIMARY KEY (user_id, role_id),
    INDEX idx_user (user_id),
    INDEX idx_role (role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';

-- 菜单表
CREATE TABLE sys_menu (
    menu_id VARCHAR(32) PRIMARY KEY,
    parent_id VARCHAR(32) DEFAULT '0' COMMENT '父菜单ID',
    menu_name VARCHAR(100) NOT NULL COMMENT '菜单名称',
    menu_type TINYINT NOT NULL COMMENT '类型:1-目录,2-菜单,3-按钮',
    permission VARCHAR(100) COMMENT '权限标识',
    route_path VARCHAR(200) COMMENT '路由路径',
    component VARCHAR(200) COMMENT '组件路径',
    menu_icon VARCHAR(50) COMMENT '菜单图标',
    sort_order INT DEFAULT 0 COMMENT '排序',
    menu_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0-禁用,1-启用',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_parent (parent_id),
    INDEX idx_permission (permission)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';

-- 角色-菜单关联表
CREATE TABLE sys_role_menu (
    role_id VARCHAR(32) NOT NULL,
    menu_id VARCHAR(32) NOT NULL,
    PRIMARY KEY (role_id, menu_id),
    INDEX idx_role (role_id),
    INDEX idx_menu (menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';

-- 机构表
CREATE TABLE sys_org (
    org_id VARCHAR(32) PRIMARY KEY,
    parent_id VARCHAR(32) DEFAULT '0' COMMENT '父机构ID',
    org_code VARCHAR(50) NOT NULL COMMENT '机构代码',
    org_name VARCHAR(100) NOT NULL COMMENT '机构名称',
    org_level TINYINT NOT NULL COMMENT '机构级别:0-总行,1-分行,2-支行,3-分理处',
    org_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0-禁用,1-启用',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_parent (parent_id),
    INDEX idx_code (org_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='机构表';

-- 授权模板表
CREATE TABLE auth_template (
    template_id VARCHAR(32) PRIMARY KEY,
    template_code VARCHAR(32) NOT NULL UNIQUE COMMENT '模板代码',
    template_name VARCHAR(128) NOT NULL COMMENT '模板名称',
    template_type TINYINT NOT NULL COMMENT '类型:1-交易授权,2-金额授权',
    auth_type TINYINT NOT NULL COMMENT '方式:1-同步,2-异步',
    template_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0-停用,1-启用',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_code (template_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='授权模板表';

-- 授权级别配置表
CREATE TABLE auth_level_config (
    config_id VARCHAR(32) PRIMARY KEY,
    template_id VARCHAR(32) NOT NULL COMMENT '所属模板ID',
    level_no TINYINT NOT NULL COMMENT '授权级别序号',
    auth_level TINYINT NOT NULL COMMENT '需要的授权级别',
    min_amount DECIMAL(18,2) COMMENT '金额区间起始值',
    max_amount DECIMAL(18,2) COMMENT '金额区间结束值',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_template (template_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='授权级别配置表';

-- 授权任务表
CREATE TABLE auth_task (
    task_id VARCHAR(32) PRIMARY KEY,
    task_no VARCHAR(50) NOT NULL UNIQUE COMMENT '任务编号',
    business_type VARCHAR(50) NOT NULL COMMENT '业务类型',
    business_id VARCHAR(32) NOT NULL COMMENT '业务ID',
    business_no VARCHAR(50) NOT NULL COMMENT '业务单号',
    template_id VARCHAR(32) COMMENT '授权模板ID',
    operator_id VARCHAR(32) NOT NULL COMMENT '经办人ID',
    operator_name VARCHAR(50) NOT NULL COMMENT '经办人姓名',
    org_id VARCHAR(32) NOT NULL COMMENT '经办机构ID',
    current_level TINYINT NOT NULL COMMENT '当前待审批级别',
    task_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:1-待审批,2-审批中,3-已完成,4-已拒绝',
    screenshots TEXT COMMENT '业务截图(JSON数组)',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_business (business_type, business_id),
    INDEX idx_operator (operator_id),
    INDEX idx_status (task_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='授权任务表';

-- 授权审批记录表
CREATE TABLE auth_approve_record (
    record_id VARCHAR(32) PRIMARY KEY,
    task_id VARCHAR(32) NOT NULL COMMENT '任务ID',
    approver_id VARCHAR(32) COMMENT '审批人ID',
    approver_name VARCHAR(50) COMMENT '审批人姓名',
    auth_level TINYINT NOT NULL COMMENT '审批级别',
    approve_result TINYINT NOT NULL COMMENT '结果:1-通过,2-拒绝',
    approve_opinion VARCHAR(500) COMMENT '审批意见',
    approve_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_task (task_id),
    INDEX idx_approver (approver_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='授权审批记录表';
```

#### 3.2.2 客户管理表（admin_crm）

```sql
-- 客户表
CREATE TABLE crm_customer (
    customer_id VARCHAR(32) PRIMARY KEY,
    customer_name VARCHAR(200) NOT NULL COMMENT '客户名称',
    cert_type TINYINT NOT NULL COMMENT '证件类型:1-身份证,2-营业执照,3-其他',
    cert_no VARCHAR(50) NOT NULL COMMENT '证件号码',
    phone VARCHAR(20) COMMENT '联系电话',
    address VARCHAR(500) COMMENT '联系地址',
    customer_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0-禁用,1-启用',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_cert (cert_type, cert_no),
    INDEX idx_name (customer_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='客户表';

-- 客户签约表
CREATE TABLE crm_contract (
    contract_id VARCHAR(32) PRIMARY KEY,
    contract_no VARCHAR(50) NOT NULL UNIQUE COMMENT '签约编号',
    customer_id VARCHAR(32) NOT NULL COMMENT '客户ID',
    contract_type VARCHAR(32) NOT NULL COMMENT '签约类型',
    contract_amount DECIMAL(18,2) NOT NULL COMMENT '签约金额',
    effective_date DATE NOT NULL COMMENT '生效日期',
    expiry_date DATE COMMENT '失效日期',
    contract_status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:1-待生效,2-生效中,3-已失效,4-已终止',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_no (contract_no),
    INDEX idx_customer (customer_id),
    INDEX idx_status (contract_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='客户签约表';
```

---

## 4 API接口设计

### 4.1 统一返回格式

```java
// 统一返回对象
public class Result<T> implements Serializable {
    private static final long serialVersionUID = 1L;
    
    private int code;
    private String msg;
    private T data;
    private long timestamp;
    
    public static <T> Result<T> success() {
        return success(null);
    }
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
    
    public static <T> Result<T> error(String msg) {
        return error(500, msg);
    }
    
    public static <T> Result<T> error(int code, String msg) {
        Result<T> result = new Result<>();
        result.setCode(code);
        result.setMsg(msg);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
}

// 分页返回对象
public class PageResult<T> implements Serializable {
    private List<T> list;
    private long total;
    private long page;
    private long pageSize;
    
    public static <T> PageResult<T> of(List<T> list, long total, long page, long pageSize) {
        PageResult<T> result = new PageResult<>();
        result.setList(list);
        result.setTotal(total);
        result.setPage(page);
        result.setPageSize(pageSize);
        return result;
    }
}
```

### 4.2 公共接口

```java
// 登录请求
public class LoginRequest {
    @NotBlank(message = "用户名不能为空")
    private String username;
    
    @NotBlank(message = "密码不能为空")
    private String password;
}

// 登录响应
public class LoginResponse {
    private String token;
    private UserInfo userInfo;
}

// 当前用户响应
public class UserInfo {
    private String userId;
    private String username;
    private String realName;
    private String orgId;
    private String orgName;
    private Integer authLevel;
    private List<String> permissions;
}
```

### 4.3 系统管理接口

```java
// 用户管理
@RestController
@RequestMapping("/api/sys/user")
public class SysUserController {
    
    @GetMapping("/list")
    public Result<PageResult<UserVO>> list(UserQuery query) {
        // 分页查询用户列表
    }
    
    @PostMapping
    public Result<Void> create(@Valid @RequestBody CreateUserCommand command) {
        // 创建用户
    }
    
    @PutMapping
    public Result<Void> update(@Valid @RequestBody UpdateUserCommand command) {
        // 修改用户
    }
    
    @DeleteMapping("/{id}")
    public Result<Void> delete(@PathVariable String id) {
        // 删除用户
    }
}

// 用户Query
public class UserQuery {
    private String username;
    private String orgId;
    private Integer status;
    private Integer page = 1;
    private Integer pageSize = 20;
}

// 用户VO
public class UserVO {
    private String userId;
    private String username;
    private String realName;
    private String orgId;
    private String orgName;
    private List<RoleVO> roles;
    private Integer authLevel;
    private Integer status;
    private LocalDateTime createTime;
}
```

### 4.4 授权管理接口

```java
// 授权任务Controller
@RestController
@RequestMapping("/api/auth/task")
public class AuthTaskController {
    
    @GetMapping("/todo")
    public Result<PageResult<AuthTaskVO>> getTodoList(AuthTaskQuery query) {
        // 获取待办任务列表
    }
    
    @GetMapping("/done")
    public Result<PageResult<AuthTaskVO>> getDoneList(AuthTaskQuery query) {
        // 获取已办任务列表
    }
    
    @GetMapping("/history")
    public Result<PageResult<AuthTaskVO>> getHistoryList(AuthTaskQuery query) {
        // 获取授权历史列表
    }
    
    @GetMapping("/{id}")
    public Result<AuthTaskDetailVO> getDetail(@PathVariable String id) {
        // 获取任务详情（含截图）
    }
    
    @PostMapping("/approve")
    public Result<Void> approve(@Valid @RequestBody ApproveCommand command) {
        // 审批通过
    }
    
    @PostMapping("/reject")
    public Result<Void> reject(@Valid @RequestBody RejectCommand command) {
        // 审批拒绝
    }
}

// 审批命令
public class ApproveCommand {
    @NotBlank(message = "任务ID不能为空")
    private String taskId;
    
    @NotBlank(message = "审批意见不能为空")
    private String opinion;
}

// 拒绝命令
public class RejectCommand {
    @NotBlank(message = "任务ID不能为空")
    private String taskId;
    
    @NotBlank(message = "拒绝原因不能为空")
    private String reason;
}

// 任务详情VO
public class AuthTaskDetailVO {
    private String taskId;
    private String taskNo;
    private String businessType;
    private String businessNo;
    private String operatorName;
    private String orgName;
    private Integer currentLevel;
    private List<String> screenshots;
    private List<ApproveRecordVO> records;
}
```

---

## 5 安全与国密设计

### 5.1 国密算法工具类

```java
// SM3密码加密
@Component
public class SM3Util {
    
    private static final String ALGORITHM = "SM3";
    private static final String CHARSET = "UTF-8";
    
    public static String encrypt(String data) {
        try {
            SM3Digest digest = new SM3Digest();
            byte[] bytes = data.getBytes(CHARSET);
            digest.update(bytes, 0, bytes.length);
            byte[] result = new byte[digest.getDigestSize()];
            digest.doFinal(result, 0);
            return Hex.toHexString(result);
        } catch (Exception e) {
            throw new BusinessException("SM3加密失败");
        }
    }
    
    public static boolean verify(String rawPassword, String encryptedPassword) {
        return encrypt(rawPassword).equalsIgnoreCase(encryptedPassword);
    }
}

// SM4数据加密（用于敏感数据）
@Component
public class SM4Util {
    
    private static final String ALGORITHM = "SM4/ECB/PKCS5Padding";
    private final String key = "0123456789abcdef"; // 16字节密钥
    
    public String encrypt(String data) {
        try {
            Cipher cipher = Cipher.getInstance(ALGORITHM);
            SecretKeySpec secretKey = new SecretKeySpec(key.getBytes(), "SM4");
            cipher.init(Cipher.ENCRYPT_MODE, secretKey);
            byte[] encrypted = cipher.doFinal(data.getBytes("UTF-8"));
            return Base64.getEncoder().encodeToString(encrypted);
        } catch (Exception e) {
            throw new BusinessException("SM4加密失败");
        }
    }
    
    public String decrypt(String encryptedData) {
        try {
            Cipher cipher = Cipher.getInstance(ALGORITHM);
            SecretKeySpec secretKey = new SecretKeySpec(key.getBytes(), "SM4");
            cipher.init(Cipher.DECRYPT_MODE, secretKey);
            byte[] decrypted = cipher.doFinal(Base64.getDecoder().decode(encryptedData));
            return new String(decrypted, "UTF-8");
        } catch (Exception e) {
            throw new BusinessException("SM4解密失败");
        }
    }
}
```

### 5.2 JWT认证

```java
// JWT工具类
@Component
public class JwtUtil {
    
    private static final String SECRET_KEY = "customer-admin-secret-key";
    private static final long EXPIRE_TIME = 24 * 60 * 60 * 1000; // 24小时
    
    public String generateToken(String userId, String username) {
        Date now = new Date();
        Date expireDate = new Date(now.getTime() + EXPIRE_TIME);
        
        return Jwts.builder()
                .subject(userId)
                .claim("username", username)
                .issuedAt(now)
                .expiration(expireDate)
                .signWith(SignatureAlgorithm.HS256, SECRET_KEY)
                .compact();
    }
    
    public Claims parseToken(String token) {
        return Jwts.parser()
                .setSigningKey(SECRET_KEY)
                .parseClaimsJws(token)
                .getBody();
    }
    
    public String getUserIdFromToken(String token) {
        return parseToken(token).getSubject();
    }
    
    public boolean isTokenExpired(String token) {
        try {
            Date expiration = parseToken(token).getExpiration();
            return expiration.before(new Date());
        } catch (Exception e) {
            return true;
        }
    }
}
```

### 5.3 Spring Security配置

```java
// Security配置
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/login", "/api/doc/**", "/swagger-ui/**").permitAll()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
        
        return http.build();
    }
}

// JWT认证过滤器
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, 
                                    FilterChain filterChain) throws ServletException, IOException {
        String token = getTokenFromRequest(request);
        
        if (StringUtils.hasText(token) && !jwtUtil.isTokenExpired(token)) {
            String userId = jwtUtil.getUserIdFromToken(token);
            // 加载用户信息并设置认证
            UsernamePasswordAuthenticationToken authentication = 
                new UsernamePasswordAuthenticationToken(userId, null, new ArrayList<>());
            SecurityContextHolder.getContext().setAuthentication(authentication);
        }
        
        filterChain.doFilter(request, response);
    }
}
```

---

## 6 Dubbo服务设计

### 6.1 服务接口定义

```java
// 公共模块中定义接口
package com.customer.admin.dubbo.api;

public interface CustomerDubboService {
    
    CustomerDTO getCustomerById(String customerId);
    
    List<CustomerDTO> getCustomerList(CustomerQuery query);
    
    void createCustomer(CreateCustomerCommand command);
    
    void updateCustomer(UpdateCustomerCommand command);
    
    void deleteCustomer(String customerId);
}
```

### 6.2 服务实现

```java
// CRM服务实现
@DubboService(version = "1.0.0", group = "crm-service")
public class CustomerDubboServiceImpl implements CustomerDubboService {
    
    @Autowired
    private CustomerApplicationService customerApplicationService;
    
    @Override
    public CustomerDTO getCustomerById(String customerId) {
        return customerApplicationService.getById(customerId);
    }
    
    @Override
    public List<CustomerDTO> getCustomerList(CustomerQuery query) {
        return customerApplicationService.queryList(query);
    }
    
    @Override
    public void createCustomer(CreateCustomerCommand command) {
        customerApplicationService.create(command);
    }
    
    @Override
    public void updateCustomer(UpdateCustomerCommand command) {
        customerApplicationService.update(command);
    }
    
    @Override
    public void deleteCustomer(String customerId) {
        customerApplicationService.delete(customerId);
    }
}
```

### 6.3 服务调用

```java
// 运营平台调用CRM服务
@DubboReference(version = "1.0.0", group = "crm-service")
private CustomerDubboService customerDubboService;

// 在应用服务中调用
public void submitAuth(String contractId) {
    // 获取客户信息
    CustomerDTO customer = customerDubboService.getCustomerById(contractId);
    
    // 创建授权任务
    authApplicationService.createAuthTask(customer);
}
```

---

## 7 验收标准

- [ ] 项目结构符合DDD四层架构
- [ ] 数据库表设计完整，包含所有业务表
- [ ] API接口设计规范，统一返回格式
- [ ] 国密SM3/SM4工具类完整
- [ ] JWT认证流程完整
- [ ] Spring Security配置正确
- [ ] Dubbo服务接口定义规范
- [ ] Nacos配置正确
- [ ] Sentinel限流规则配置

---

**设计人**：后端开发
**日期**：2026-03-04
**版本**：v1.0
